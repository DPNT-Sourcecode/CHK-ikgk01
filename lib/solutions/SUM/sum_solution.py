
class SumSolution:
    
    def compute(self, x, y):
        return x + y

    def sum(self, x, y):
        def __init__(self, x y):
            self.x = x
            self.y = y
            return compute(self.x, self.y)
